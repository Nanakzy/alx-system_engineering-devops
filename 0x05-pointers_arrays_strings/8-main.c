#include "main.h"

/**
 * main - check the code for
 *
 * Return: Always 0.
 */
int main(void)
{
	int array[5];

	array[0] = 98;
	array[0] = 402;
	array[0] = -198;
	array[0] = 298;
	array[0] = -1024;
	print_array(array, 5);
	return (0);
}
